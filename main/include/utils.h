#ifndef __utils_h_included__
#define __utils_h_included__

#include <string.h>
#include <stdlib.h>

extern char *get_line(char *text, char *fragment);
extern char *str_replace(char *orig, char *rep, char *with);

#endif